# Fonts

Drop `.woff2`, `.woff`, and `.ttf` files here. `font-face.css` already
declares them — replace the filenames if you swap in a different family.

Binary files are NOT committed when `assets/font/*.woff2` exceeds 100 KB.
Use `git lfs` or a release attachment instead.
